/*
 * calculatrice.c
 *
 *  Created on: Nov 30, 2025
 *      Author: root
 */
#include "Calculatrice.h"

void add(Operandes *op)
{
    op->resultat = op->a + op->b;
}

void sub(Operandes *op)
{
    op->resultat = op->a - op->b;
}

void mul(Operandes *op)
{
    op->resultat = op->a * op->b;
}
